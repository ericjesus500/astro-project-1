---
const {color, label} = Astro.props
---

<button style={`background-color: ${color}`}>
    {label}
</button>

<style>
    button{
        padding: 1em;
        border: none;
        color: white;
        cursor: pointer;
        border-radius: 10px;
    }
    
</style>